# 問題 3.16 解答

## Benの手続き

```scheme
(define (count-pairs x)
  (if (not (pair? x))
      0
      (+ (count-pairs (car x))
         (count-pairs (cdr x))
         1)))
```

## 3を返すもの

```
x -> (* *)->(* *)->(* /)
      |      |      |
      V      V      V
      a      b      c
```

## 4を返すもの

```
      +-------------+
      |             |
      |             V
x -> (* *)->(* *)->(* /)
             |      |
             V      V
             b      c
```

## 7を返すもの

```
             +------+
             |      |
             |      V
x -> (* *)->(* *)->(* /)
      |      ^      |
      |      |      V
      +------+      a
```

## 何も返さないもの

```
      +---------------+
      |               |
      V               |
x -> (* *)->(* *)->(* *)
      |      |      |
      V      V      V
      a      b      c
```
